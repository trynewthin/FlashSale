# Vercel Serverless API proxy target (no trailing slash)
BACKEND_ORIGIN=https://your-backend-domain.example.com

# Keep default to use same-origin /api/v1 via Vercel proxy
# VITE_USER_API_BASE_URL=/api/v1

